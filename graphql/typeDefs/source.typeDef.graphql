# Query , Mutation
type Query {
  getSource: String!
  getRoute: String!
}

type Mutation {
  print(data: DataInput): Data
}

# Args
input DataInput {
  name: String!
}

# Response

type Data {
  name: String!
  code: String!
}
